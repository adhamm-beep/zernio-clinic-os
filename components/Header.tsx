"use client";

import { useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { Bell, CheckCheck, Globe2, LogOut } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { useLocale } from "@/components/LocaleProvider";
import { createClient } from "@/lib/supabase/client";
import { useClinic } from "@/features/clinic/hooks/useClinic";
import { getStaffNotifications, readAllNotifications, readNotification } from "@/features/enterprise/api/enterprise.api";
import GlobalSearch from "@/components/GlobalSearch";
import MobileNavigation from "@/components/MobileNavigation";

const titles: Record<string, [string, string]> = {
  dashboard:["Dashboard","اللوحة الرئيسية"],customers:["Patients","المرضى"],appointments:["Appointments","المواعيد"],calendar:["Calendar","التقويم"],treatments:["Care & treatments","العناية والعلاجات"],payments:["Payments","المدفوعات"],accounting:["Finance & accounting","المالية والمحاسبة"],"price-list":["Services & prices","الخدمات والأسعار"],inventory:["Inventory","المخزون"],staff:["Team","الفريق"],marketing:["Marketing","التسويق"],reports:["Reports","التقارير"],"ask-zernio":["Ask Panthera","اسأل بانثيرا"],"ai-agents":["Automation","الأتمتة"],"patient-catalogs":["Patient catalogs","علامات المرضى والإحالات"],settings:["Settings","الإعدادات"],enterprise:["Enterprise management","إدارة المؤسسة"],support:["Technical support","الدعم الفني"],tasks:["Tasks","المهام"],messages:["Patient messages","رسائل المرضى"],logs:["Audit log","سجل التدقيق"],"patient-app":["Patient app","تطبيق المرضى"],"follow-ups":["Patient reminders","تنبيهات المرضى"],
};
const arTerms: Record<string,string>={requested:"بانتظار التأكيد",pending:"قيد الانتظار",confirmed:"مؤكد",booked:"محجوز",arrived:"تم تسجيل الوصول",completed:"مكتمل",cancelled:"ملغي",canceled:"ملغي",no_show:"لم يحضر",paid:"مدفوع",partially_paid:"مدفوع جزئيًا",unpaid:"غير مدفوع",refunded:"مسترد"};
function localized(value:string,isArabic:boolean){const parts=value.split(/\s*(?:·|\|)\s*/u).map(x=>x.trim()).filter(Boolean);let output=value;if(parts.length>1){const ar=parts.find(x=>/[\u0600-\u06ff]/.test(x)),en=parts.find(x=>!/[\u0600-\u06ff]/.test(x));output=isArabic?(ar??parts[0]):(en??parts.at(-1)??value);}if(isArabic)for(const[term,label]of Object.entries(arTerms))output=output.replace(new RegExp(`\\b${term}\\b`,"gi"),label);return output;}
function hrefFor(item:{href:string|null;type:string}){if(item.href?.startsWith("/")&&!item.href.startsWith("//"))return item.href;const t=item.type.toLowerCase();if(/appointment|booking|request/.test(t))return"/appointments";if(/invoice|payment|billing/.test(t))return"/payments";if(/customer|patient/.test(t))return"/customers";if(/treatment|care/.test(t))return"/treatments";if(/follow/.test(t))return"/follow-ups";if(/inventory|stock/.test(t))return"/inventory";if(/marketing|lead|campaign/.test(t))return"/marketing";if(/message/.test(t))return"/messages";if(/task/.test(t))return"/tasks";if(/staff|employee|attendance|schedule/.test(t))return"/staff";return"/dashboard";}

export default function Header(){
  const router=useRouter(),pathname=usePathname(),qc=useQueryClient();const{clinic,selectedBranch}=useClinic();const{isArabic,toggleLocale,text}=useLocale();const[open,setOpen]=useState(false);
  const current=useQuery({queryKey:["current-staff-header"],queryFn:async()=>{const{data,error}=await createClient().rpc("current_staff_header");if(error)throw error;return(data?.[0]??null)as{staff_name:string|null;email:string|null}|null;},staleTime:300_000});
  const clinicId=clinic?.id??0,branchId=selectedBranch?.id;const notifications=useQuery({queryKey:["staff-notifications",clinicId,branchId??"all"],queryFn:()=>getStaffNotifications(clinicId,branchId),enabled:clinicId>0,refetchInterval:10_000,refetchIntervalInBackground:true});
  const items=notifications.data??[],unread=items.filter(x=>!x.is_read).length,key=pathname.split("/").filter(Boolean)[0]??"dashboard",title=titles[key]??["Panthera","بانثيرا"];
  async function signOut(){await createClient().auth.signOut();router.replace("/login");router.refresh();}
  async function openItem(item:(typeof items)[number]){if(!item.is_read)await readNotification(item.id);await qc.invalidateQueries({queryKey:["staff-notifications"]});setOpen(false);router.push(hrefFor(item));}
  async function readAll(){await readAllNotifications(clinicId);await qc.invalidateQueries({queryKey:["staff-notifications"]});}
  return <header className="sticky top-0 z-30 border-b border-slate-200/80 bg-[#f7f8f7]/90 px-3 backdrop-blur-xl md:px-4" dir={isArabic?"rtl":"ltr"}><div className="flex h-[60px] items-center gap-2"><MobileNavigation/><div className="min-w-0 flex-1"><h1 className="truncate text-lg font-black text-slate-900">{isArabic?title[1]:title[0]}</h1><p className="truncate text-[11px] text-slate-500">{clinic?.name??text("Panthera Clinics","عيادات بانثيرا")}{selectedBranch?` · ${selectedBranch.name}`:""}</p></div><div className="hidden text-end sm:block"><p className="max-w-40 truncate text-sm font-black text-slate-900">{current.data?.staff_name||text("User","مستخدم")}</p><p className="max-w-40 truncate text-[10px] text-slate-500">{current.data?.email}</p></div><GlobalSearch/><button type="button" onClick={toggleLocale} className="h-10 rounded-xl border bg-white px-3 text-xs font-black text-slate-700 shadow-sm"><Globe2 className="me-1 inline size-4"/>{isArabic?"EN":"عربي"}</button><div className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} aria-label={text("Notifications","الإشعارات")} className="relative grid size-10 place-items-center rounded-xl border bg-white text-slate-600 shadow-sm"><Bell className="size-[18px]"/>{unread>0&&<span className="absolute -end-1.5 -top-1.5 grid min-w-5 place-items-center rounded-full bg-rose-600 px-1 text-[10px] font-black leading-5 text-white ring-2 ring-white">{unread>99?"99+":unread}</span>}</button>{open&&<div className="absolute end-0 top-12 z-50 w-[min(92vw,390px)] overflow-hidden rounded-2xl border bg-white shadow-2xl"><div className="flex items-center justify-between border-b p-4"><div><strong>{text("System notifications","إشعارات النظام")}</strong><p className="text-xs text-slate-500">{text(`${unread} unread`,`${unread} غير مقروء`)}</p></div>{unread>0&&<button type="button" onClick={()=>void readAll()} className="inline-flex items-center gap-1 text-xs font-bold text-indigo-700"><CheckCheck className="size-4"/>{text("Read all","قراءة الكل")}</button>}</div><div className="max-h-[420px] overflow-y-auto">{items.length===0?<p className="p-8 text-center text-sm text-slate-500">{text("No notifications yet.","لا توجد إشعارات حتى الآن.")}</p>:items.map(item=><button type="button" key={item.id} onClick={()=>void openItem(item)} className={`block w-full border-b p-4 text-start hover:bg-slate-50 ${item.is_read?"bg-white":"bg-emerald-50/70"}`}><span className="flex items-start gap-3">{!item.is_read&&<i className="mt-1.5 size-2 shrink-0 rounded-full bg-emerald-500"/>}<span><strong className="block text-sm text-slate-950">{localized(item.title,isArabic)}</strong><span className="mt-1 block text-xs leading-5 text-slate-600">{localized(item.message,isArabic)}</span><time className="mt-2 block text-[10px] text-slate-400">{new Intl.DateTimeFormat(isArabic?"ar-SA-u-nu-latn":"en-SA",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Riyadh"}).format(new Date(item.created_at))}</time></span></span></button>)}</div></div>}</div><button type="button" onClick={signOut} title={text("Sign out","تسجيل الخروج")} className="grid size-10 place-items-center rounded-xl border bg-white text-slate-600 shadow-sm hover:text-rose-600"><LogOut className="size-[18px]"/></button></div></header>;
}
