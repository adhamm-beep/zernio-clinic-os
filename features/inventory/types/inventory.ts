export type Supplier = { id: number; name: string; phone: string | null; email: string | null; tax_number: string | null; is_active: boolean };
export type Product = { id: number; name: string; sku: string | null; barcode: string | null; category: string | null; unit: string; current_stock: number; minimum_stock: number; unit_cost: number; batch_number: string | null; expiry_date: string | null; supplier_id: number | null; service_variant_id: number | null; supplier?: { name: string } | null; is_active: boolean };
export type PurchaseOrder = { id: number; order_number: string; status: string; expected_at: string | null; received_at: string | null; supplier?: { name: string } | null; items?: Array<{ id: number; product_id: number; quantity: number; received_quantity: number; unit_cost: number }> };
export type Movement = { id: number; movement_type: string; quantity: number; unit_cost: number; notes: string | null; occurred_at: string; product?: { name: string; unit: string } | null; doctor?: { staff_name: string } | null; service?: { name: string } | null };
export type InventoryData = { products: Product[]; suppliers: Supplier[]; orders: PurchaseOrder[]; movements: Movement[] };
